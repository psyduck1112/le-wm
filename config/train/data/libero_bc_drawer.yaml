dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  cache_dir: /home/yikang/git/le-wm/data
  frameskip: 1
  name: libero_bc_drawer_v2
  keys_to_load:
    - pixels
    - eye_in_hand        # M1: 腕部相机, 进视觉 emb (与 pixels 同走图像预处理)
    - action
    - proprio            # 仍只 cache, 不进 emb (M1 先不用 proprio 旁路)
  keys_to_cache:
    - action
    - proprio
