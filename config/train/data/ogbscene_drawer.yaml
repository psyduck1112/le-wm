dataset:
  name: ogbench/scene_drawer
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 2
  keys_to_load:
    - pixels
    - action
    - observation
  keys_to_cache:
    - action
    - observation
  keys_to_merge:
    proprio: observation
