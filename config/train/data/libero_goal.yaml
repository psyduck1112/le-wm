dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  cache_dir: C:/Users/RaymondZh/Desktop/github/le-wm/datasets
  frameskip: 1
  name: libero_goal
  keys_to_load:
    - pixels
    - action
    - proprio
  keys_to_cache:
    - action
    - proprio