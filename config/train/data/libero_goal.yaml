dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  cache_dir: /root/autodl-tmp/stable-wm
  frameskip: 1
  name: libero_goal
  keys_to_load:
    - pixels
    - action
    - proprio
  keys_to_cache:
    - action
    - proprio