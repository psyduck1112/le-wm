defaults:
  - launcher: local
  - solver: cem
  - _self_

world:
  env_name: single-task-libero-demo-v0
  num_envs: 1
  max_episode_steps: 50
  history_size: 3
  frame_skip: 1
  goal_conditioned: True
  demo_hdf5_path: /root/autodl-tmp/LIBERO/datasets/libero_goal/open_drawer.hdf5
  bddl_file_name: null
  camera_heights: 128
  camera_widths: 128
  goal_offset: ${eval.goal_offset}
  proprio_key: ${eval.proprio_key}

dataset:
  keys_to_cache:
    - action
    - proprio

seed: 42
policy: /root/autodl-tmp/stable-wm/outputs/lewm_libero_goal_epoch_97

plan_config:
  horizon: 7
  receding_horizon: 5
  action_block: 1

eval:
  num_eval: 5
  img_size: 224
  goal_offset: 100
  max_steps: 50
  proprio_key: robot_states
  demo_ids: null
  output_dir: single_task_open_drawer
